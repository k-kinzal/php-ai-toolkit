Rustで言うpub(crate)みたいな機能を作りたいです。PHPには公開範囲指定がないので、Docコメントによる制御になると思います。もし過去のPHPDoc系仕様でちょうど良いものがあれば参考にしてください。個人的に欲しいのは外部から呼び出し不可、同一ネームスペース以外は呼び出し不可、自身が属する親、祖父などから以外は呼び出し不可みたいなのです。検査器を作り禁止された呼び出しがあればエラーにするという機構を作成してください。mainブランチで作業し、完了したらコミットしてください。